name: moneyhub_mobile
description: "MoneyHub Mobile - cliente Flutter do MoneyHub (finanças pessoais)."
publish_to: "none"

# Versão gerenciada automaticamente pelo release-please (SemVer).
# Não altere manualmente: use Conventional Commits (feat/fix/...).
version: 0.2.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
